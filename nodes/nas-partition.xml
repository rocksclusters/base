<?xml version="1.0" standalone="no"?>

<kickstart>

<description>
Define a partitioning layout for NAS nodes.
</description>

<!-- If you want custom partitioning for NAS appliances, define here by
     replacing this node in the graph.  If you also replace custom-partition
     this node's pre section is evaluated first.   -->
<pre>
echo "rocks manual" > /tmp/user_partition_info
</pre>

</kickstart> 

